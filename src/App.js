import { BrowserRouter, Route, Switch } from "react-router-dom";
import './App.css';

function App() {
	return (
        <BrowserRouter>
            <Switch>
            	{/*<Route exact path="/" component={ }></Route>*/}
            </Switch>
        </BrowserRouter>
    );
}

export default App;
